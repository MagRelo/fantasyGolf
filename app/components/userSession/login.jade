div(data-ng-controller="LoginCtrl")
    form.form(name='form', ng-submit='login(form)', novalidate='novalidate')

        legend Login

        .form-group
            label Email
            input.form-control(type='email', name='email', ng-model='user.email', value="matt@aol.com")

        .form-group
            label Password
            input.form-control(type='password', name='password', ng-model='user.password', value="asdf")

        .form-group.has-error
            p.help-block(ng-show='form.email.$error.required && form.password.$error.required && submitted')
                | Please enter your email and password.
            p.help-block {{ errors.other }}

        button.btn.btn-lg.btn-primary(type='submit')  Sign in
        span.clearfix
