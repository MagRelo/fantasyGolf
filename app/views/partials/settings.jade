h3.text-primary Account Settings
div(data-ng-controller="SettingsCtrl")
    .well
        form.form-horizontal(name='SettingsForm', ng-submit='changePassword(form)', novalidate='')

            .form-group
                label.control-label.col-sm-3 New Email
                div.col-sm-7
                    input.form-control(type='email', name='password', ng-model='user.newEmail', mongoose-error='')
                    p.help-block(ng-show='form.password.$error.mongoose')
                        | {{ errors.other }}

            .form-group
                label.control-label.col-sm-3 Current Password
                div.col-sm-7
                    input.form-control(type='password', name='password', ng-model='user.oldPassword', mongoose-error='')
                    p.help-block(ng-show='form.password.$error.mongoose')
                        | {{ errors.other }}

            .form-group
                label.control-label.col-sm-3 New Password
                div.col-sm-7
                    input.form-control(type='password', name='newPassword', ng-model='user.newPassword', ng-minlength='3', required='')
                    p.help-block(ng-show='(form.newPassword.$error.minlength || form.newPassword.$error.required) && (form.newPassword.$dirty || submitted)')
                        | Password must be at least 3 characters.

            div.text-center
                p.help-block  {{ message }}

                button.btn.btn-success(type='submit', data-ng-show='SettingsForm.$dirty') Save changes

                .clearfix
